/**
 * Functional constants only — role names are managed in the database.
 * Do NOT add role name constants here.
 */
export class RoleConstants {
  static readonly ALL_PERMISSIONS = '*' as const;
}
